const shapes = require('./shapes');

describe('Shape generator', () => {

    it("returns a circle when prompted 'circle'");
    
    it("returns a triangle when prompted 'triangle'");

    it("returns a square when prompted 'square'");
})